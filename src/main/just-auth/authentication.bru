meta {
  name: /authentication
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/api/v1/auth/authenticate
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@example.com",
    "password": "password123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
